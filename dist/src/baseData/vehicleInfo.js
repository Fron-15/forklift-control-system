
(function() {
    define(['app'], function(app) {
        app.controller('vehicleInfoCtrl', vehicleInfoCtrl);

        vehicleInfoCtrl.$inject = ['$scope'];

        function vehicleInfoCtrl($scope) {

        }
    });
})();
