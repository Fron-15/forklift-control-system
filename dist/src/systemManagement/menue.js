
(function() {
    define(['app'], function(app) {
        app.controller('menueCtrl', menueCtrl);

        menueCtrl.$inject = ['$scope'];

        function menueCtrl($scope) {

        }
    });
})();
