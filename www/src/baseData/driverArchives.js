
(function() {
    define(['app'], function(app) {
        app.controller('driverArchivesCtrl', driverArchivesCtrl);

        driverArchivesCtrl.$inject = ['$scope'];

        function driverArchivesCtrl($scope) {

        }
    });
})();
