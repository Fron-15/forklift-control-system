(function() {
    define(['app'], function(app) {
        app.controller('driverWorkTimeChartCtrl', driverWorkTimeChartCtrl);

        driverWorkTimeChartCtrl.$inject = ['$scope'];

        function driverWorkTimeChartCtrl($scope) {
            var vm = this;
        }
    });
})();